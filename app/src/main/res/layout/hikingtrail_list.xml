<?xml version="1.0" encoding="utf-8"?>
<!--modify by  self 20160924: add hikingtrail_list.xml-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:orientation="vertical" >

    <FrameLayout
        android:layout_width="fill_parent"
        android:layout_height="0dip"
        android:layout_weight="1" >

        <!--
             Here is the list. Since we are using a ListActivity, we
             have to call it "@android:id/list" so ListActivity will
             find it
        -->

        <!--modify by self 20160921: add android:id="@android:id/list"
        to resolve Your content must have a ListView whose id attribute is 'android.R.id.list
        -->
        <ListView
            android:id="@android:id/list"
            android:layout_width="fill_parent"
            android:layout_height="fill_parent"
            android:background="@drawable/r_back"
            android:divider="@drawable/r_divider"
            android:drawSelectorOnTop="false" />

        <!-- Here is the view to show if the list is empty -->

    </FrameLayout>

</LinearLayout>